# `apps/api`

Reserved for Savant's dedicated control-plane API once runtime direction is settled.

## Current status

This directory is intentionally code-free until the Rust vs Python on Vercel assessment is complete.

Until then, any minimal control-plane endpoints should remain inside `apps/web` so the frontend can move without locking the repo into an early backend choice.
